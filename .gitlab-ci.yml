default:
  image: klakegg/hugo:alpine-ci

variables:
  GIT_SUBMODULE_STRATEGY: recursive

pages:
  script:
  - rm -rf public
  - hugo --ignoreCache
  artifacts:
    paths:
    - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
